package com.passengerapp.main;

import android.content.Context;
import android.support.v4.widget.DrawerLayout;
import android.util.AttributeSet;

/**
 * TODO: document your custom view class.
 */
public class MainMenuDrawableLayout extends DrawerLayout {

    public MainMenuDrawableLayout(Context context) {
        super(context);
    }

    public MainMenuDrawableLayout(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    public MainMenuDrawableLayout(Context context, AttributeSet attrs, int defStyle) {
        super(context, attrs, defStyle);
    }
}
